package core

import "github.com/karanshah229/gistsync/internal/domain"

// GistInfo represents summary information about a remote gist
type GistInfo = domain.GistInfo

// Provider defines the interface for sync backends
type Provider = domain.Provider
